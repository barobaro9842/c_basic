#include <stdio.h>
int main()
{
    // printf("\"The truth is... \nI am Ironman.\"");
    
    int x, y, z;
    
    x = 1;
    y = 2;
    z = x + y;

    // printf("The answer is %i", 7);
    printf("%i + %i = %i", x,y,z);
    return 0;
}