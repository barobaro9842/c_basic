// int main(void) //main은 시작점, void가 있어도 되고 없어도 되고 (입력이 없을 때)
// {

//     int x; // variable declaration, 정수 하나를 담을만한 메모리 공간을 확보했고, 그 공간은 x라는 이름으로 접근할 수 있다.
//     int y;
//     int z;

//     x = 1; // assignment, 숫자 1을 변수 x가 사용하고 있는 메모리에 대입한다.
//     y = 2;
    
//     z = x + y;

    

//     return 0; //return을 하는 순간, 그 뒤의 일들은 무시한다. = end점 표시
// }

int main(void)
{
    int x = 1, y = 2;
    int z;
    z = x+y;
     
    return 0;
}